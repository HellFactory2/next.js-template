const config = {
  i18n: {
    locales: ['th'],
    defaultLocale: 'th',
    domains: [
      {
        domain: 'localhost',
        defaultLocale: 'th',
      },
    ],
  },
};

module.exports = config;
